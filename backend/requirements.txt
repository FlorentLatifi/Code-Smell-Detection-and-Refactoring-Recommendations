# Runtime dependencies. Development and CI tooling lives in requirements-dev.txt.
# Pinned exactly so that a reported result can be reproduced at the version that
# produced it (ENGINEERING.md 3.5).

# Java front end.
tree-sitter==0.26.0
tree-sitter-java==0.23.5

# Approach B. scikit-learn is BSD-3-Clause and carries the four things writing
# ourselves would be substantial: grouped cross-validation, the two tree
# ensembles, permutation importance, and the metrics. The rest are its own
# transitive pins, listed so the environment is fully determined.
scikit-learn==1.9.0
numpy==2.5.2
scipy==1.18.1
joblib==1.5.3
threadpoolctl==3.6.0

# Shtresa web. Vetem transport: validim, serializim dhe hartë gabimesh.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5

# Pinat tranzitive te shtreses web. Te palistuara, ato zgjidheshin ne kohen e
# instalimit, dhe me 2026-09-03 anyio 4.15.0 e shpalli te vjetruar aliasin
# `anyio.abc.BlockingPortal` qe starlette e prek gjate importit: paralajmerimi
# behet gabim nen `filterwarnings = error` dhe CI-ja u prish pa ndryshuar asnje
# rresht kodi. Nje mjedis "plotesisht i percaktuar" i perfshin edhe keto.
starlette==1.6.0
anyio==4.14.2
idna==3.19
annotated-doc==0.0.5
annotated-types==0.8.0
pydantic-core==2.46.5
typing-extensions==4.16.0
typing-inspection==0.4.4
click==8.5.0
h11==0.16.0
